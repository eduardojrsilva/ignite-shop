import { createStitches } from '@stitches/react';

export const {
  config,
  styled,
  css,
  globalCss,
  getCssText,
  keyframes,
  theme,
  createTheme
} = createStitches({
  theme: {
    colors: {
      
    }
  }
});